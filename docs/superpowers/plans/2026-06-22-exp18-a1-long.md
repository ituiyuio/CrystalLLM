# Exp 18: A1 Long Training on Extended Data — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Verify [[exp17-cmt-phase-transition]] decision by training A1 (lr=3e-5) for 12k step on v23+v28 merged data. Determine if CMT architecture is salvageable via training mechanism (H2) or truly architecturally broken (H1).

**Architecture:** Reuse `experiments/v49_pre/cmt_clean.py` (untouched). New merged data loader (v23+v28). Extended `exp17_phase_transition.py` for 12k step + 5 checkpoints. Reuse `exp17_aggregate.py` logic with 5-step variant. v1.1 evaluation standard throughout.

**Tech Stack:** PyTorch 2.x, RTX 5090 32GB, bnb 8-bit AdamW (optional), v23_train.parquet + v28_train.parquet, char-level vocab=2261.

**Spec:** `docs/superpowers/specs/2026-06-22-exp18-a1-extended-training.md`

---

## File Structure

**Create:**
- `experiments/v49_pre/exp18_data_validate.py` — v23 vs v28 distribution validation (KL divergence, vocab overlap)
- `experiments/v49_pre/exp18_data_merge.py` — v23+v28 merged loader
- `experiments/v49_pre/exp18_a1_long.py` — 12k step training script (extends exp17_phase_transition.py)
- `experiments/v49_pre/exp18_aggregate.py` — 5-checkpoint evaluation + classification
- `experiments/v49_pre/tests/test_exp18_data_merge.py` — unit tests for merged loader
- `docs/experiments/2026-06-22-exp18-a1-long-training-results.md` — final report

**Reuse (do NOT modify):**
- `experiments/v49_pre/cmt_clean.py` — CMT50MClean (0-bug fair control)
- `experiments/v49_pre/exp_runner.py` — `train_step`, `VOCAB_SIZE`
- `experiments/v49_pre/data_loader.py` — `_load_vocab`, `_make_token_windows`
- `experiments/v49_pre/exp17_metrics.py` — 3 new metrics
- `experiments/v49_pre/exp17_checkpoint.py` — checkpoint save/load
- `experiments/v49_pre/exp17_v49_50m_calibrate.json` — V49 50M calibration reference
- `experiments/v49_pre/exp17_aggregate.py` — `classify_state` (re-use, don't copy)

---

## Task 1: Validate v23 vs v28 Distribution Compatibility

**Files:**
- Create: `experiments/v49_pre/exp18_data_validate.py`
- Create: `experiments/v49_pre/tests/test_exp18_data_validate.py`

- [ ] **Step 1: Create test file with 3 failing tests**

```python
# experiments/v49_pre/tests/test_exp18_data_validate.py
"""Unit tests for v23 vs v28 distribution validation."""
import math
from experiments.v49_pre.exp18_data_validate import (
    char_frequency_distribution,
    kl_divergence,
    vocab_overlap_ratio,
    top_k_char_overlap,
)


def test_char_frequency_distribution_v28():
    """v28_train first 1000 samples should have char frequency summing to 1."""
    dist = char_frequency_distribution(
        parquet_path="crystalllm/data/processed/v28_train.parquet",
        n_samples=1000,
    )
    assert abs(sum(dist.values()) - 1.0) < 1e-6
    # Most common chars should be ' ', 'e', 't', 'n', etc.
    assert max(dist, key=dist.get) in [' ', 'e', 't', 'n', '\n']


def test_kl_divergence_v23_v28_under_threshold():
    """KL(v23 || v28) should be < 0.1 if distributions are compatible."""
    p_v23 = char_frequency_distribution(
        parquet_path="crystalllm/data/processed/v23_train.parquet", n_samples=1000,
    )
    p_v28 = char_frequency_distribution(
        parquet_path="crystalllm/data/processed/v28_train.parquet", n_samples=1000,
    )
    # Align vocabs
    all_chars = set(p_v23.keys()) | set(p_v28.keys())
    p_v23_aligned = {c: p_v23.get(c, 1e-10) for c in all_chars}
    p_v28_aligned = {c: p_v28.get(c, 1e-10) for c in all_chars}
    kl = kl_divergence(p_v23_aligned, p_v28_aligned)
    # Empirical: if v23 and v28 are both code/agentic data, KL should be small
    # v23 is older; allow up to 0.5 for some drift, but flag if > 1.0
    print(f"KL(v23 || v28) = {kl:.4f}")
    # Test passes if KL < 1.0 (loose threshold; print helps debug)
    assert kl < 1.0, f"KL divergence {kl:.4f} too large, distributions incompatible"


def test_vocab_overlap_v23_v28():
    """Char vocab overlap v23 ∩ v28 / v23 ∪ v28 should be > 0.9."""
    v23_chars = set(char_frequency_distribution(
        parquet_path="crystalllm/data/processed/v23_train.parquet", n_samples=1000,
    ).keys())
    v28_chars = set(char_frequency_distribution(
        parquet_path="crystalllm/data/processed/v28_train.parquet", n_samples=1000,
    ).keys())
    overlap = vocab_overlap_ratio(v23_chars, v28_chars)
    assert overlap > 0.9, f"vocab overlap {overlap:.4f} too low"
```

- [ ] **Step 2: Run tests to verify they fail (ImportError expected)**

Run: `cd D:/CrystaLLM && .venv/Scripts/python -m pytest experiments/v49_pre/tests/test_exp18_data_validate.py -v`
Expected: FAIL with `ImportError`

- [ ] **Step 3: Create `exp18_data_validate.py`**

```python
# experiments/v49_pre/exp18_data_validate.py
"""Validate v23 vs v28 distribution compatibility for Exp 18 data merge.

Computes:
  - char frequency distribution
  - KL divergence between two distributions
  - vocab overlap ratio
  - top-k char overlap

Usage:
    cd D:/CrystaLLM && python -m experiments.v49_pre.exp18_data_validate
"""
import math
from collections import Counter
from pathlib import Path

import pandas as pd


def char_frequency_distribution(parquet_path: str, n_samples: int = 1000) -> dict:
    """Compute normalized char frequency distribution from first n_samples of parquet.

    Returns: dict mapping char -> probability (sums to 1).
    """
    df = pd.read_parquet(parquet_path)
    texts = df["text"].head(n_samples).tolist()
    counter = Counter()
    total = 0
    for text in texts:
        for c in text:
            counter[c] += 1
            total += 1
    if total == 0:
        return {}
    return {c: count / total for c, count in counter.items()}


def kl_divergence(p: dict, q: dict) -> float:
    """Compute KL(p || q) = sum p(x) * log(p(x) / q(x)) in bits.

    Both p and q must have the same keys. q[x] must be > 0 for all x.
    """
    kl = 0.0
    for x in p:
        p_x = p[x]
        q_x = q[x]
        if p_x > 0:
            kl += p_x * math.log2(p_x / q_x)
    return kl


def vocab_overlap_ratio(set_a: set, set_b: set) -> float:
    """|A ∩ B| / |A ∪ B| (Jaccard index)."""
    if not set_a and not set_b:
        return 1.0
    intersection = set_a & set_b
    union = set_a | set_b
    return len(intersection) / len(union)


def top_k_char_overlap(dist_a: dict, dist_b: dict, k: int = 100) -> float:
    """Fraction of top-k chars in dist_a that also appear in top-k of dist_b."""
    top_a = set(c for c, _ in sorted(dist_a.items(), key=lambda x: -x[1])[:k])
    top_b = set(c for c, _ in sorted(dist_b.items(), key=lambda x: -x[1])[:k])
    if not top_a:
        return 0.0
    return len(top_a & top_b) / len(top_a)


def main():
    v23_path = "crystalllm/data/processed/v23_train.parquet"
    v28_path = "crystalllm/data/processed/v28_train.parquet"

    print("Computing char frequency distributions (n_samples=5000)...")
    p_v23 = char_frequency_distribution(v23_path, n_samples=5000)
    p_v28 = char_frequency_distribution(v28_path, n_samples=5000)
    print(f"  v23: {len(p_v23)} unique chars")
    print(f"  v28: {len(p_v28)} unique chars")

    # Align vocabs
    all_chars = set(p_v23.keys()) | set(p_v28.keys())
    p_v23_aligned = {c: p_v23.get(c, 1e-10) for c in all_chars}
    p_v28_aligned = {c: p_v28.get(c, 1e-10) for c in all_chars}

    print("\n=== Distribution Compatibility Report ===\n")
    kl = kl_divergence(p_v23_aligned, p_v28_aligned)
    print(f"KL(v23 || v28): {kl:.4f} bits")
    if kl < 0.1:
        verdict_kl = "EXCELLENT (compatible)"
    elif kl < 0.5:
        verdict_kl = "GOOD (merge OK with monitoring)"
    elif kl < 1.0:
        verdict_kl = "BORDERLINE (consider v28-only fallback)"
    else:
        verdict_kl = "INCOMPATIBLE (use v28-only)"
    print(f"  Verdict: {verdict_kl}")

    v23_chars = set(p_v23.keys())
    v28_chars = set(p_v28.keys())
    overlap = vocab_overlap_ratio(v23_chars, v28_chars)
    print(f"\nVocab overlap: {overlap:.4f}")
    if overlap > 0.95:
        verdict_vocab = "EXCELLENT"
    elif overlap > 0.9:
        verdict_vocab = "GOOD"
    else:
        verdict_vocab = "POOR (chars missing in v28 may corrupt training)"
    print(f"  Verdict: {verdict_vocab}")

    top_overlap = top_k_char_overlap(p_v23, p_v28, k=100)
    print(f"\nTop-100 char overlap: {top_overlap:.4f}")
    if top_overlap > 0.9:
        verdict_top = "EXCELLENT"
    else:
        verdict_top = "POOR (v23 has different char usage pattern)"
    print(f"  Verdict: {verdict_top}")

    # Final decision
    print(f"\n=== Final Decision ===")
    if kl < 0.5 and overlap > 0.9 and top_overlap > 0.85:
        decision = "MERGE_OK"
        print("  v23 + v28 can be merged. Proceed with merged training.")
    else:
        decision = "USE_V28_ONLY"
        print("  Distribution incompatible. Use v28 only with 8k step (single epoch).")
    print(f"  Decision: {decision}")

    # Save report
    out_path = Path("experiments/v49_pre/results/exp18_data_validation.json")
    out_path.parent.mkdir(parents=True, exist_ok=True)
    import json
    with open(out_path, "w") as f:
        json.dump({
            "kl_divergence": kl,
            "vocab_overlap": overlap,
            "top_100_overlap": top_overlap,
            "v23_unique_chars": len(p_v23),
            "v28_unique_chars": len(p_v28),
            "decision": decision,
        }, f, indent=2)
    print(f"\nReport saved to {out_path}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd D:/CrystaLLM && .venv/Scripts/python -m pytest experiments/v49_pre/tests/test_exp18_data_validate.py -v`
Expected: 3 tests PASS

- [ ] **Step 5: Run validation script**

Run: `cd D:/CrystaLLM && .venv/Scripts/python -m experiments.v49_pre.exp18_data_validate 2>&1 | tee experiments/v49_pre/logs/exp18_data_validate.log | tail -30`
Expected: outputs KL divergence, vocab overlap, top-100 overlap, and final decision (MERGE_OK or USE_V28_ONLY)

- [ ] **Step 6: Commit**

```bash
cd D:/CrystaLLM && git add experiments/v49_pre/exp18_data_validate.py experiments/v49_pre/tests/test_exp18_data_validate.py experiments/v49_pre/results/exp18_data_validation.json experiments/v49_pre/logs/exp18_data_validate.log
git -c user.email="claude@local" -c user.name="Claude" commit -m "exp18: validate v23 vs v28 distribution compatibility (KL, vocab, top-k overlap)"
```

---

## Task 2: Write v23+v28 Merged Data Loader

**Files:**
- Create: `experiments/v49_pre/exp18_data_merge.py`
- Create: `experiments/v49_pre/tests/test_exp18_data_merge.py`

- [ ] **Step 1: Create test file with 2 failing tests**

```python
# experiments/v49_pre/tests/test_exp18_data_merge.py
"""Unit tests for v23+v28 merged data loader."""
import numpy as np

from experiments.v49_pre.exp18_data_merge import (
    load_merged_texts,
    build_merged_loader,
)


def test_load_merged_texts_concatenates_v23_and_v28():
    """Merged texts should contain both v23 and v28 samples."""
    texts = load_merged_texts(
        v23_path="crystalllm/data/processed/v23_train.parquet",
        v28_path="crystalllm/data/processed/v28_train.parquet",
    )
    assert len(texts) > 6317  # > v23 alone (6317)
    assert len(texts) > 69307  # > v28 alone (69307)
    # Should be approximately v23 + v28 = 75624
    assert abs(len(texts) - 75624) < 100


def test_build_merged_loader_returns_correct_batch_shape():
    """Merged loader should yield batches of shape (batch, seq_len)."""
    loader = build_merged_loader(
        v23_path="crystalllm/data/processed/v23_train.parquet",
        v28_path="crystalllm/data/processed/v28_train.parquet",
        batch_size=4, seq_len=128,
    )
    batch = next(iter(loader))[0]
    assert batch.shape == (4, 128)
```

- [ ] **Step 2: Run tests to verify they fail (ImportError expected)**

Run: `cd D:/CrystaLLM && .venv/Scripts/python -m pytest experiments/v49_pre/tests/test_exp18_data_merge.py -v`
Expected: FAIL with `ImportError`

- [ ] **Step 3: Create `exp18_data_merge.py`**

```python
# experiments/v49_pre/exp18_data_merge.py
"""v23 + v28 merged data loader for Exp 18.

Concatenates v23 and v28 train sets, then reuses _make_token_windows
from data_loader.py to create (B, T) windows for training.

Usage:
    from experiments.v49_pre.exp18_data_merge import build_merged_loader
    loader = build_merged_loader(batch_size=8, seq_len=512)
"""
from pathlib import Path
from typing import Optional

import numpy as np
import pandas as pd

PROJECT_ROOT = Path(__file__).resolve().parents[2]

from experiments.v49_pre.data_loader import _load_vocab, _make_token_windows


def load_merged_texts(
    v23_path: str = "crystalllm/data/processed/v23_train.parquet",
    v28_path: str = "crystalllm/data/processed/v28_train.parquet",
) -> list:
    """Load and concatenate v23 and v28 train texts.

    Returns: list of text strings, v23 first then v28.
    """
    df_v23 = pd.read_parquet(v23_path)
    df_v28 = pd.read_parquet(v28_path)
    v23_texts = df_v23["text"].tolist()
    v28_texts = df_v28["text"].tolist()
    return v23_texts + v28_texts


def build_merged_loader(
    v23_path: str = "crystalllm/data/processed/v23_train.parquet",
    v28_path: str = "crystalllm/data/processed/v28_train.parquet",
    batch_size: int = 8,
    seq_len: int = 512,
    shuffle: bool = True,
    seed: int = 42,
    max_samples: Optional[int] = None,
):
    """Build DataLoader over merged v23+v28 train set.

    Returns DataLoader that yields (tokens,) tuples, tokens: (B, T) int64.
    """
    from torch.utils.data import DataLoader, TensorDataset
    texts = load_merged_texts(v23_path, v28_path)
    stoi, _ = _load_vocab()
    rng = np.random.default_rng(seed)
    indices = np.arange(len(texts))
    if max_samples is not None and max_samples < len(texts):
        indices = rng.choice(len(texts), size=max_samples, replace=False)
    windows = _make_token_windows(texts, indices, stoi, seq_len, rng)
    dataset = TensorDataset(torch_from_numpy_windows(windows))
    return DataLoader(dataset, batch_size=batch_size, shuffle=shuffle, num_workers=0)


def torch_from_numpy_windows(windows: np.ndarray):
    import torch
    return torch.from_numpy(windows)
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd D:/CrystaLLM && .venv/Scripts/python -m pytest experiments/v49_pre/tests/test_exp18_data_merge.py -v`
Expected: 2 tests PASS

- [ ] **Step 5: Commit**

```bash
cd D:/CrystaLLM && git add experiments/v49_pre/exp18_data_merge.py experiments/v49_pre/tests/test_exp18_data_merge.py
git -c user.email="claude@local" -c user.name="Claude" commit -m "exp18: v23+v28 merged data loader with unit tests"
```

---

## Task 3: 12k Step A1 Long Training Script

**Files:**
- Create: `experiments/v49_pre/exp18_a1_long.py`

- [ ] **Step 1: Create 12k step training script (reuses exp17_phase_transition.py logic)**

```python
# experiments/v49_pre/exp18_a1_long.py
"""Exp 18: A1 long training (lr=3e-5) for 12k step on v23+v28 merged data.

Reuses exp17_phase_transition.py infrastructure with:
  - 12k total steps (vs 4k in Exp 17)
  - 5 checkpoints: 4000, 6000, 8000, 10000, 12000 (vs 4 in Exp 17)
  - lr=3e-5, dropout=0.1 (A1 config from Exp 17)

Usage:
    cd D:/CrystaLLM && python -m experiments.v49_pre.exp18_a1_long
"""
import argparse
import json
import math
import sys
import time
from pathlib import Path

import torch

PROJECT_ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(PROJECT_ROOT))

from experiments.v49_pre.cmt_clean import CMT50MClean
from experiments.v49_pre.data_loader import _load_vocab
from experiments.v49_pre.exp_runner import VOCAB_SIZE, train_step
from experiments.v49_pre.exp18_data_merge import build_merged_loader
from experiments.v49_pre.exp17_checkpoint import save_phase_transition_ckpt


# A1 config from Exp 17
LR = 3e-5
DROPOUT = 0.1
N_TOTAL_STEPS = 12000
BATCH_SIZE = 8
SEQ_LEN = 512
CHECKPOINT_STEPS = [4000, 6000, 8000, 10000, 12000]


def evaluate_ppl_heldout_on_device(model, val_parquet, stoi, device, seq_len=512, max_texts=50, max_windows_per_text=3):
    """Local device-aware held-out PPL evaluator."""
    import pandas as pd
    import numpy as np
    import torch.nn as nn
    df = pd.read_parquet(val_parquet)
    texts = df["text"].tolist()
    loss_fn = nn.CrossEntropyLoss(reduction="sum")
    total_loss, total_tokens = 0.0, 0
    n_texts = min(len(texts), max_texts)
    model.eval()
    with torch.no_grad():
        for i in range(n_texts):
            text = texts[i]
            ids = [stoi.get(c, 0) for c in text]
            n_windows = len(ids) // seq_len
            if n_windows == 0:
                continue
            ids = ids[: n_windows * seq_len]
            arr = np.asarray(ids, dtype=np.int64).reshape(-1, seq_len)
            n_eval = min(n_windows, max_windows_per_text)
            for j in range(n_eval):
                x = torch.from_numpy(arr[j:j+1]).to(device)
                x_in, y = x[:, :-1], x[:, 1:]
                logits = model(x_in)
                loss = loss_fn(logits.reshape(-1, logits.size(-1)), y.reshape(-1))
                total_loss += loss.item()
                total_tokens += y.numel()
    model.train()
    if total_tokens == 0:
        return None
    return math.exp(total_loss / max(total_tokens, 1))


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--ckpt_dir", default="experiments/v49_pre/results/exp18_ckpts")
    parser.add_argument("--val_parquet", default="crystalllm/data/processed/v28_val.parquet")
    args = parser.parse_args()

    ckpt_dir = Path(args.ckpt_dir)
    ckpt_dir.mkdir(parents=True, exist_ok=True)

    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
    print(f"=== Exp 18: A1 Long Training (12k step on v23+v28) ===")
    print(f"Device: {device}, lr={LR}, dropout={DROPOUT}, total_steps={N_TOTAL_STEPS}")

    model = CMT50MClean(
        vocab_size=VOCAB_SIZE, d_model=640, n_layers=8, n_heads=8,
        kan_dim=96, dropout=DROPOUT,
    ).to(device)
    n_params = sum(p.numel() for p in model.parameters() if p.requires_grad)
    print(f"CMT-Clean params: {n_params:,}")

    optimizer = torch.optim.AdamW(model.parameters(), lr=LR)
    from torch.optim.lr_scheduler import LambdaLR
    def lr_lambda(step):
        warmup = 500
        if step < warmup:
            return step / warmup
        progress = (step - warmup) / max(1, N_TOTAL_STEPS - warmup)
        return 0.5 * (1 + math.cos(math.pi * progress)) * (1 - 0.1) + 0.1
    scheduler = LambdaLR(optimizer, lr_lambda)

    train_loader = build_merged_loader(batch_size=BATCH_SIZE, seq_len=SEQ_LEN, shuffle=True)
    stoi, _ = _load_vocab()

    t_start = time.time()
    val_ppl_curve = []
    for step in range(1, N_TOTAL_STEPS + 1):
        batch = next(iter(train_loader))[0].to(device)
        loss = train_step(model, batch, optimizer)
        scheduler.step()

        if step in CHECKPOINT_STEPS:
            val_ppl = evaluate_ppl_heldout_on_device(
                model, args.val_parquet, stoi, device, seq_len=SEQ_LEN,
                max_texts=50, max_windows_per_text=3,
            )
            ckpt_path = ckpt_dir / f"exp18_a1_long_step{step}.pt"
            save_phase_transition_ckpt(
                model, str(ckpt_path), step=step, config_label="A1_long",
                val_ppl=val_ppl,
            )
            elapsed = time.time() - t_start
            print(f"  step {step}: val_ppl={val_ppl:.4f} | ckpt saved | {elapsed:.0f}s elapsed")
            val_ppl_curve.append((step, val_ppl))

    # Save val_ppl curve
    out_path = Path("experiments/v49_pre/results/exp18_val_ppl_curve.json")
    with open(out_path, "w") as f:
        json.dump({"val_ppl_curve": val_ppl_curve, "config": "A1_long", "total_steps": N_TOTAL_STEPS}, f, indent=2)
    print(f"\nval_ppl curve saved to {out_path}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Commit training script (before running)**

```bash
cd D:/CrystaLLM && git add experiments/v49_pre/exp18_a1_long.py
git -c user.email="claude@local" -c user.name="Claude" commit -m "exp18: 12k step A1 long training script (extends exp17)"
```

---

## Task 4: Run A1 Long Training (12k step, ~55 min)

**Files:**
- (No new files; reuse Task 3 script)

- [ ] **Step 1: Run training in background**

Run: `cd D:/CrystaLLM && .venv/Scripts/python -m experiments.v49_pre.exp18_a1_long 2>&1 | tee experiments/v49_pre/logs/exp18_a1_long_train.log`
Expected: 5 checkpoints saved at step 4000/6000/8000/10000/12000, ~55 min

- [ ] **Step 2: Verify checkpoints exist**

Run: `cd D:/CrystaLLM && ls -la experiments/v49_pre/results/exp18_ckpts/exp18_a1_long_step*.pt`
Expected: 5 .pt files, each ~280-300MB

- [ ] **Step 3: Commit log only**

```bash
cd D:/CrystaLLM && git add experiments/v49_pre/logs/exp18_a1_long_train.log
git -c user.email="claude@local" -c user.name="Claude" commit -m "exp18: A1 long training complete - 12k step, 5 checkpoints"
```

---

## Task 5: 5-Checkpoint Evaluation + Classification

**Files:**
- Create: `experiments/v49_pre/exp18_aggregate.py`

- [ ] **Step 1: Create aggregate script (reuses exp17 logic with 5 checkpoints)**

```python
# experiments/v49_pre/exp18_aggregate.py
"""Aggregate Exp 18 results: classify each of 5 A1_long checkpoints.

Reuses exp17_aggregate.py infrastructure:
  - Loads V49 50M calibration as reference
  - For each of 5 checkpoints, computes v1.0 + v1.1 metrics
  - Classifies as real_lm | memorizer | underfit
  - Outputs decision: H2 (real_lm found) | H1 (none real_lm) | H2_EDGE (boundary)

Usage:
    cd D:/CrystaLLM && python -m experiments.v49_pre.exp18_aggregate
"""
import json
import math
import sys
from pathlib import Path

import numpy as np
import torch

PROJECT_ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(PROJECT_ROOT))

from experiments.v49_pre.cmt_clean import CMT50MClean
from experiments.v49_pre.data_loader import _load_vocab, _make_token_windows, load_v28_full
from experiments.v49_pre.exp_runner import VOCAB_SIZE
from experiments.v49_pre.exp16_cmt_clean import (
    build_full_loader, eval_generation_diversity,
    is_locally_coherent, detect_repetition_run,
)
from experiments.v49_pre.exp17_metrics import (
    n_gram_entropy, top_1_confidence_stats, val_train_ppl_gap,
)
from experiments.v49_pre.exp17_checkpoint import load_phase_transition_ckpt
from experiments.v49_pre.exp17_aggregate import evaluate_ppl_heldout_local, classify_state


CHECKPOINT_STEPS = [4000, 6000, 8000, 10000, 12000]


def evaluate_checkpoint(ckpt_path, val_parquet, stoi, itos, device):
    """Same as exp17_aggregate.evaluate_checkpoint, returns full metrics."""
    model = CMT50MClean(
        vocab_size=VOCAB_SIZE, d_model=640, n_layers=8, n_heads=8,
        kan_dim=96, dropout=0.1,
    )
    metadata = load_phase_transition_ckpt(model, ckpt_path)
    model = model.to(device)
    model.eval()

    val_ppl = evaluate_ppl_heldout_local(
        model, val_parquet, stoi, device, seq_len=512, max_texts=50, max_windows_per_text=3,
    )
    gen_results = eval_generation_diversity(model, stoi, itos, device)
    n_coherent, n_repetition = 0, 0
    all_divs = []
    for pname, pdata in gen_results.items():
        for temp_key, td in pdata.items():
            all_divs.append(td["diversity"])
            if is_locally_coherent(td["text_sample"]):
                n_coherent += 1
            rep, _ = detect_repetition_run(td["text_sample"])
            if rep:
                n_repetition += 1
    avg_diversity = sum(all_divs) / len(all_divs) if all_divs else 0.0

    texts = load_v28_full()
    rng = np.random.default_rng(metadata["step"] + hash(ckpt_path) % 1000)
    indices = rng.choice(len(texts), size=8)
    windows = _make_token_windows(texts, indices, stoi, 512, rng)
    x = torch.from_numpy(windows[:2]).to(device)
    with torch.no_grad():
        logits = model(x[:, :-1])
    logits_flat = logits.reshape(-1, VOCAB_SIZE)
    entropy = n_gram_entropy(logits_flat)
    mean_conf, std_conf = top_1_confidence_stats(logits_flat)

    train_indices = rng.choice(len(windows), size=min(8, len(windows)), replace=False)
    train_windows = windows[train_indices]
    train_x = torch.from_numpy(train_windows).to(device)
    loss_fn = torch.nn.CrossEntropyLoss(reduction="sum")
    with torch.no_grad():
        train_logits = model(train_x[:, :-1])
        train_loss = loss_fn(train_logits.reshape(-1, VOCAB_SIZE), train_x[:, 1:].reshape(-1))
        train_ppl = math.exp(train_loss.item() / train_x[:, 1:].numel())
    gap = val_train_ppl_gap(val_ppl=val_ppl, train_ppl=train_ppl)

    return {
        "step": metadata["step"],
        "config": metadata["config"],
        "val_ppl": val_ppl,
        "train_ppl_estimate": train_ppl,
        "val_train_ppl_gap": gap,
        "diversity": avg_diversity,
        "n_coherent": n_coherent,
        "n_repetition": n_repetition,
        "n_gram_entropy_bits": entropy,
        "top_1_confidence_mean": mean_conf,
        "top_1_confidence_std": std_conf,
    }


def main():
    device = torch.device("cuda" if torch.cuda.is_available() else "cpu")
    stoi, _ = _load_vocab()
    itos = {v: k for k, v in stoi.items()}
    val_parquet = "crystalllm/data/processed/v28_val.parquet"

    cal_path = Path("experiments/v49_pre/results/exp17_v49_50m_calibrate.json")
    with open(cal_path) as f:
        calibration = json.load(f)
    print(f"V49 50M calibration: val_ppl={calibration['val_ppl']:.3f}, "
          f"entropy={calibration['n_gram_entropy_bits']:.3f} bit, "
          f"conf={calibration['top_1_confidence_mean']:.4f}")

    ckpt_dir = Path("experiments/v49_pre/results/exp18_ckpts")
    all_results = []
    for step in CHECKPOINT_STEPS:
        ckpt_path = ckpt_dir / f"exp18_a1_long_step{step}.pt"
        if not ckpt_path.exists():
            print(f"WARN: missing {ckpt_path}, skipping")
            continue
        print(f"\nEvaluating A1_long step {step}...")
        metrics = evaluate_checkpoint(str(ckpt_path), val_parquet, stoi, itos, device)
        state = classify_state(metrics, calibration)
        metrics["state"] = state
        all_results.append(metrics)
        print(f"  val_ppl={metrics['val_ppl']:.4f}, entropy={metrics['n_gram_entropy_bits']:.3f}, "
              f"conf={metrics['top_1_confidence_mean']:.4f}, gap={metrics['val_train_ppl_gap']:.3f} -> {state}")

    # Decision tree
    states = [r["state"] for r in all_results]
    has_real_lm = "real_lm" in states
    has_memorizer = "memorizer" in states
    all_underfit = all(s == "underfit" for s in states)

    if has_real_lm:
        decision = "H2_CONFIRMED"
        real_lm_steps = [r["step"] for r in all_results if r["state"] == "real_lm"]
        detail = (
            f"At least one checkpoint reached real_lm state (steps {real_lm_steps}). "
            f"v50 canonical = CMT-clean + lr=3e-5 + 12k step on v23+v28 merged data."
        )
    elif all_underfit:
        decision = "H1_HARDENED"
        detail = (
            f"All 5 checkpoints are underfit (val_ppl > 3.0). A1 at 12k step still hasn't converged. "
            f"v50 should pivot to V49 1.2B + BPE + external data (Wikipedia)."
        )
    elif has_memorizer and not has_real_lm:
        decision = "H1_CONFIRMED"
        memorizer_steps = [r["step"] for r in all_results if r["state"] == "memorizer"]
        detail = (
            f"A1 eventually memorizes at step(s) {memorizer_steps} (val_ppl < 1.5, entropy < 0.3 bit). "
            f"CMT architecture is fundamentally broken on char-level next-token. "
            f"v50 = V49 1.2B + BPE + external data."
        )
    else:
        decision = "H2_EDGE"
        detail = (
            f"Mixed states: {states}. Boundary case. v50 = V49 1.2B + BPE primary, "
            f"v51 = retry CMT-clean with different config if time permits."
        )

    summary = {
        "calibration": calibration,
        "all_checkpoints": all_results,
        "decision": decision,
        "detail": detail,
    }

    out_path = Path("experiments/v49_pre/results/exp18_aggregate.json")
    with open(out_path, "w", encoding="utf-8") as f:
        json.dump(summary, f, indent=2, ensure_ascii=False, default=str)
    print(f"\n=== Decision: {decision} ===")
    print(f"  {detail}")
    print(f"  Saved to {out_path}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Run aggregate analysis**

Run: `cd D:/CrystaLLM && .venv/Scripts/python -m experiments.v49_pre.exp18_aggregate 2>&1 | tee experiments/v49_pre/logs/exp18_aggregate.log | tail -30`
Expected: 5 checkpoints evaluated, decision output

- [ ] **Step 3: Commit**

```bash
cd D:/CrystaLLM && git add experiments/v49_pre/exp18_aggregate.py experiments/v49_pre/results/exp18_aggregate.json experiments/v49_pre/logs/exp18_aggregate.log
git -c user.email="claude@local" -c user.name="Claude" commit -m "exp18: aggregate 5-checkpoint classification with H1/H2 decision"
```

---

## Task 6: Write Experiment Results Report

**Files:**
- Create: `docs/experiments/2026-06-22-exp18-a1-long-training-results.md`

- [ ] **Step 1: Generate report from aggregate JSON**

Run: `cd D:/CrystaLLM && .venv/Scripts/python <<'EOF'
import json
from pathlib import Path

d = json.load(open('experiments/v49_pre/results/exp18_aggregate.json'))
cal = d['calibration']
val_path = Path("experiments/v49_pre/results/exp18_val_ppl_curve.json")
val_curve = json.load(open(val_path)) if val_path.exists() else {"val_ppl_curve": []}

report = f'''# Exp 18: A1 Long Training on v23+v28 Results (2026-06-22)

> 承接 [[exp17-cmt-phase-transition]] 决策验证, 在 12k step + 合并数据上验证 A1 (lr=3e-5) 是否进入真 LM 状态.

## 0. 结论先行

**决策**: `{d["decision"]}`

{d["detail"]}

## 1. 数据规模对比

| 数据集 | 样本数 | 估计 tokens | 12k step epoch 数 |
|--------|--------|-------------|---------------------|
| v28 单独 | 69,307 | ~35M | 1.37 epoch |
| v23+v28 合并 | 75,624 | ~38M | 1.26 epoch |

**Layer 1 边际改善**: 撞 1 epoch 边界从 1.37 epoch 降到 1.26 epoch, 不根本解决问题.

## 2. V49 50M 校准 (复用)

| 指标 | V49 50M 值 | 真 LM 阈值 (本文档) |
|------|------------|---------------------|
| val_ppl | {cal["val_ppl"]:.4f} | 1.5-3.0 |
| n-gram entropy | {cal["n_gram_entropy_bits"]:.3f} bit | >= 0.63 bit |
| top-1 confidence | {cal["top_1_confidence_mean"]:.4f} | < 0.95 |
| val-train PPL gap | {cal["val_train_ppl_gap"]:.3f} | > 0 |

## 3. val_ppl 训练曲线

| Step | val_ppl |
|------|---------|
'''
for step, ppl in val_curve.get("val_ppl_curve", []):
    report += f"| {step} | {ppl:.4f} |\n"

report += '''
## 4. 检查点状态分类 (5 检查点 v1.1 评估)

| Step | val_ppl | entropy | conf | gap | state |
|------|---------|---------|------|-----|-------|
'''
for r in d['all_checkpoints']:
    report += f"| {r['step']} | {r['val_ppl']:.4f} | {r['n_gram_entropy_bits']:.3f} | {r['top_1_confidence_mean']:.4f} | {r['val_train_ppl_gap']:.3f} | {r['state']} |\n"

report += f'''
## 5. 决策树分析

**决策**: `{d["decision"]}`

'''

if d['decision'] == 'H2_CONFIRMED':
    report += '''**H2 成立**: A1 在 12k step 进入真 LM 状态. v50 canonical:
- CMT-clean + lr=3e-5 + 12k step on v23+v28 merged data
- 进一步: scale 1.2B on 合并数据 + BPE
'''
elif d['decision'] == 'H1_CONFIRMED':
    report += '''**H1 真正成立**: A1 在 12k step 仍未进入真 LM, 最终 memorizer. v50 路径:
- V49 1.2B + BPE + 外部数据 (Wikipedia 子集)
- CMT 架构在 char-level next-token 上是架构本质失败
'''
elif d['decision'] == 'H1_HARDENED':
    report += '''**H1_HARDENED**: A1 12k step 仍 underfit (val_ppl > 3.0). 关键问题: A1 是否在更长训练收敛?
- 选项 1: 跑 20k step 验证 (但 v28+v23 仍只 38M tokens, 撞 1 epoch 风险大)
- 选项 2: 接受决策, 走 V49 1.2B + BPE + 外部数据
'''
elif d['decision'] == 'H2_EDGE':
    report += '''**H2_EDGE**: 边界情况, 部分检查点在真 LM 边界. v50 优先 BPE, v51 重试 CMT.
'''

report += f'''
## 6. 与 Exp 17 对比

| 实验 | 步数 | 最佳 val_ppl | 最佳 entropy | 状态 |
|------|------|--------------|---------------|------|
| Exp 17 A1 (v28 only) | 4000 | 15.76 | 4.39 | underfit |
| Exp 18 A1_long (v23+v28) | 12000 | (this exp) | (this exp) | (this exp) |

## 7. 关键引用

- [[exp17-cmt-phase-transition]]: Exp 17 phase transition 决策
- [[exp16-cmt-clean]]: Exp 16 CMT-clean 0-bug 判决
- [[v49-scale-1-2b]]: V49 1.2B baseline
- [[lm-evaluation-standard]] v1.1: 5+3 维评估标准
- spec: `docs/superpowers/specs/2026-06-22-exp18-a1-extended-training.md`
- plan: `docs/superpowers/plans/2026-06-22-exp18-a1-long.md`
'''

Path('docs/experiments/2026-06-22-exp18-a1-long-training-results.md').write_text(report, encoding='utf-8')
print('Report written. Size:', len(report), 'chars')
EOF`
Expected: prints "Report written."

- [ ] **Step 2: Verify report exists**

Run: `cd D:/CrystaLLM && ls -la docs/experiments/2026-06-22-exp18-a1-long-training-results.md && head -15 docs/experiments/2026-06-22-exp18-a1-long-training-results.md`
Expected: file exists with markdown content

- [ ] **Step 3: Commit report**

```bash
cd D:/CrystaLLM && git add docs/experiments/2026-06-22-exp18-a1-long-training-results.md
git -c user.email="claude@local" -c user.name="Claude" commit -m "exp18: results report - 5 ckpts classified, H1/H2 decision documented"
```

---

## Task 7: Update Memory with Exp 18 Decision

**Files:**
- Modify: `C:\Users\98399\.claude\projects\D--CrystaLLM\memory\MEMORY.md`
- Create: `C:\Users\98399\.claude\projects\D--CrystaLLM\memory\2026-06-22-exp18-a1-long.md`

- [ ] **Step 1: Create Exp 18 memory file**

```markdown
---
name: exp18-a1-long
description: Exp 18 — A1 (lr=3e-5) 12k step on v23+v28 merged data. 5 ckpts (4000/6000/8000/10000/12000). 决策 H2_CONFIRMED | H1_CONFIRMED | H1_HARDENED, 决定 v50 路径.
metadata: 
  node_type: memory
  type: project
---

# Exp 18: A1 Long Training on v23+v28 (2026-06-22)

承接 [[exp17-cmt-phase-transition]] 决策验证.

## 数据规模临界点

- v28_train FULL: 69,307 samples, ~35M tokens, 1 epoch = 8,750 steps
- v23_train FULL: **6,317** samples (远小于预期 ~24k)
- **v23+v28 合并: 75,624 samples, ~38M tokens, 1 epoch = 9,500 steps**
- 12k step 撞 1 epoch 边界: v28 alone 1.37 epoch, v23+v28 1.26 epoch
- **Layer 1 (合并 v23) 边际改善有限**, 只能减少 11% 重复, 不能根本解决撞 1 epoch 问题

## 实施

- A1_long (lr=3e-5, dropout=0.1) 12k step on v23+v28 merged
- 5 检查点 (4000/6000/8000/10000/12000)
- 复用 v1.1 评估标准 + V49 50M 校准
- 复用 exp17_aggregate.classify_state

## 关键数据 (待 Exp 18 完成后填入)

[Step | val_ppl | entropy | conf | gap | state]

## 决策 (待 Exp 18 完成后填入)

[决策 + v50 路径]

## Why

承接 [[exp17-cmt-phase-transition]] 决策 H1_PARTIAL_A1_SUGGESTS_TRAINING. Exp 18 是 v50 决策树的关键实验. 如果 A1 在 12k step 进入真 LM, CMT 架构可救; 否则走 V49 1.2B + BPE + 外部数据.

## How to apply

- 任何 CMT 引用: 必须先跑 Exp 18 验证 v50 路径
- 决策后: v50 spec 基于 Exp 18 决策
- Layer 2 (Wikipedia) 在 Exp 18 H1 失败后才需要
```

- [ ] **Step 2: Update MEMORY.md index**

Add line to `C:\Users\98399\.claude\projects\D--CrystaLLM\memory\MEMORY.md`:

```
- [Exp 18 A1 Long](2026-06-22-exp18-a1-long.md) — A1 (lr=3e-5) 12k step on v23+v28 merged data, 5 ckpts. 验证 Exp 17 决策, 决定 v50 canonical 路径.
```

- [ ] **Step 3: Commit memory**

```bash
cd D:/CrystaLLM && git add C:/Users/98399/.claude/projects/D--CrystaLLM/memory/2026-06-22-exp18-a1-long.md C:/Users/98399/.claude/projects/D--CrystaLLM/memory/MEMORY.md
git -c user.email="claude@local" -c user.name="Claude" commit -m "memory: Exp 18 A1 long training - placeholder for v50 decision verification"
```

---

## Self-Review

**Spec coverage:**
- §1.1-1.2 上下文 → Task 1 (分布验证), Task 2 (合并 loader) ✓
- §3.2 训练配置 → Task 3 (脚本) + Task 4 (跑训练) ✓
- §3.3 评估指标 → Task 5 (aggregate) ✓
- §4 决策树 → Task 5 (decision logic) + Task 6 (报告) ✓
- §5 成本与风险 → Task 1-7 注释中 ✓
- §6 范围 → Task 1-7 实施中 ✓
- §7 产出 → Task 1-6 全部产出 ✓

**Placeholder scan:** No TBD/TODO/"implement later" patterns. All code blocks are complete with full implementations.

**Type consistency:**
- `exp18_data_merge.build_merged_loader` defined in Task 2, used in Task 3 ✓
- `exp17_aggregate.classify_state` and `evaluate_ppl_heldout_local` reused in Task 5 (not redefined) ✓
- `exp17_checkpoint.save_phase_transition_ckpt` used in Task 3 ✓
- CMT50MClean instantiation uses same `d_model=640, n_layers=8, n_heads=8, kan_dim=96` in Tasks 3, 5 (matches Exp 17 config) ✓
- `CHECKPOINT_STEPS = [4000, 6000, 8000, 10000, 12000]` consistent across Tasks 3, 4, 5 ✓
- A1 config (lr=3e-5, dropout=0.1) consistent across Tasks 3, 4 ✓

**No ambiguities found.**

**Execution time estimate:**
- Task 1: ~10 min (write tests + script + run validation)
- Task 2: ~5 min (write tests + loader)
- Task 3: ~5 min (write training script + commit)
- Task 4: ~55 min (12k step training)
- Task 5: ~10 min (5-checkpoint evaluation)
- Task 6: ~5 min (report generation)
- Task 7: ~5 min (memory update)
- **Total: ~95 min** (~1.6 hours)
